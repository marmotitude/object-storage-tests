{{range $remote_name, $remote := .remotes}}
  {{ if eq $remote.type "s3" }}
[{{$remote_name }}]
aws_access_key_id = {{ $remote.access_key }}
aws_secret_access_key = {{ $remote.secret_key }}

  {{end}}
{{end}}

